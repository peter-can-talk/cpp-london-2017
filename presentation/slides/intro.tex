% !TEX root = ../presentation.tex
% Background

\begin{slide}{Background}
  \begin{tikzpicture}[very thick]
    % TUM
    \onslide<2->{
      \path (0:2cm) coordinate [draw, circle, white, inner sep=0.4cm] (t)
            node {\includegraphics[scale=0.16]{tum}};
    }

    % Google
    \onslide<3->{
      \path (90:2cm) coordinate [draw, circle, white, inner sep=0.4cm] (g)
            node {\includegraphics[scale=0.05]{google}};
      \draw [->, bend right] (t) to (g);
    }

    % Bloomberg
    \onslide<4->{
      \path (180:2cm) coordinate [draw, circle, white, inner sep=0.45cm] (b)
            node {\includegraphics[scale=0.09]{bloomberg}};
      \draw [->, bend right] (g) to (b);
    }

    % Facebook
    \onslide<5->{
      \path (270:2cm) coordinate [draw, circle, white, inner sep=0.45cm] (f)
            node {\includegraphics[scale=0.0275]{fb}};
      \draw [->, bend right] (b) to (f);
    }

    \onslide<6->{
      \draw [->, bend right] (f) to (t);
    }
  \end{tikzpicture}
\end{slide}
