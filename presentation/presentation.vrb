\frametitle{Clang Tooling: libTooling}
\begin{lstlisting}[basicstyle=\large\ttfamily]
  const auto clang_lambda = [] (auto) noexcept {
      bool done = true;
      flip: done = !done;
      if (!done) goto flip;
  };
  \end{lstlisting}
