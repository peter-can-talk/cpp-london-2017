#include <vector>
#include <string>

class X { int* pointer; }; 
long * p; 
void f(short*) { };
int *q = new int[5];
std::vector<std::string::iterator>* 
	vsip = new std::vector<std::string::iterator>();
int* p_pointer;
